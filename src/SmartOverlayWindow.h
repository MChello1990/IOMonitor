#pragma once

// SmartOverlayWindow is now integrated directly into SmartMonitor.
// This header is kept for backward compatibility with the vcxproj references.
// The overlay functionality is provided by SmartMonitor::toggleOverlay() and
// the OVERLAY_CLASS window class registered within SmartMonitor.

#include "SmartMonitor.h"
